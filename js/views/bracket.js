/* ============================================================================
   BRACKET VIEW  —  the knockout tree
   ----------------------------------------------------------------------------
   Placeholder while we rebuild it from the real ESPN data (next step). The old
   demo bracket was removed when we switched to live data.
   ========================================================================== */

function renderBracket() {
  return `<div class="page">
    <h2 class="section-title">Knockout bracket</h2>
    <p class="muted">The bracket is being rebuilt from the real World Cup data —
      coming in the next update. For now, see live matches and results on
      <b>Home</b>.</p>
  </div>`;
}
